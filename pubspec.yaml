name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # ✅ Maps & Location
  mapbox_maps_flutter: ^2.21.1
  flutter_map: ^8.2.2
  latlong2: ^0.9.0
  geolocator: ^14.0.2

  # ✅ Network
  dio: ^5.4.0

  # ✅ Firebase (stable combo)
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.4.0

  # ✅ UI
  pin_code_fields: ^8.0.1

  # ✅ FIX (important for your error)
  package_info_plus: ^9.0.1  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/icons/
    - assets/images/